# Portfolio Query Skill

Allows an AI agent or browser assistant to programmatically query Nguyen Minh Hieu's work history, portfolio projects, and technical skills stack.

## Requirements
- Call the `get-portfolio-data` tool via WebMCP or MCP.
- Fetch the portfolio details as structured JSON or dynamic markdown content.
